# 👤 USER — Phase 2 ревью от лица оператора CRM

*Я оператор PRIDE. Каждый день работаю с клиентами через дашборд + Telegram bot. Мне плевать на код. Мне важно — работает ли всё после деплоя.*

## ❓ Мои 3 ГЛАВНЫХ вопроса

### 1. Какая теперь цена ВТБ?
В чате клиента/work-чате если AI спросят "сколько за ВТБ?":
- **pricing.md**: 400$ (primary source, не тронут)
- **storage.lk_prices['втб']**: ⚠️ зависит от того что SIMBA настроил в runtime
- Все другие места (leo_brain/deals/about/accounting/faq/policy/scenarios): хардкодов **больше нет**, AI идёт в pricing.md

**Что я ожидаю после деплоя:** AI отвечает "400$" клиенту. Если SIMBA уже менял через бота `прайс ВТБ 450` — то 450$. Если storage пуст — fallback в pricing.md = 400$.

**🚨 РИСК для меня:** AI **никогда не должен молчать** про цену. Если storage.lk_prices['втб'] вообще нет → AI скажет "не знаю цену" → клиент уйдёт. Worker #2 не проверил это.

### 2. Если tron_monitor не стартует на cold Railway — я смогу принимать клиентов?
**Worker #1 фикс:** retry 10 попыток с exponential backoff (max 30s × 10 = 5 минут). Если не получилось — monitor disabled, бот продолжает.

**Что я увижу:** ничего критичного — welcome-бот стартует, AI-userbot работает, дашборд открывается. **НО:** автоплатежи USDT TRC20 не работают! Менеджер на дашборде нажмёт "Выплатить" → ничего не произойдёт.

**🚨 РИСК для меня:** **никто меня не предупредит** что monitor лёг. Только в Railway logs пишется error. Дашборд не показывает алерт. Я могу 30 минут думать что "сейчас уйдёт" а оно не уйдёт.

**Запрос:** добавить admin-нотификацию в TG owner-чат при tron warmup failure.

### 3. AI userbot отвечает правильно после извлечения _FORBIDDEN_RX в отдельный файл?
**Worker #3 фикс:** 83 паттерна вынесены в `userbot_forbidden_patterns.py`, импорт обратно совместим.

**Что я ожидаю:** AI продолжает блокировать "извините", "наша ошибка", "сколько хотите", "дроп", теги менеджеров в reply, спекуляции про команду.

**Что для меня важно:** проверить ВРУЧНУЮ после деплоя — отправить в work-chat сообщение типа "извините за ошибку" и убедиться что AI его НЕ отправит клиенту. Если отправит — это позор, репутация PRIDE.

**🚨 РИСК для меня:** Worker #3 тестил импорт через `python -c "import userbot"` (с моками). Реальный AI flow на проде не тестился.

## ✅ Что точно безопасно

- **Bug #4** (`storage._save_unlocked()` → `save()`) — теперь правильно через lock. Это исключает race condition при сохранении state. Финансовые операции (баланс/выплаты) становятся надёжнее.
- **Bug #5** (`guard_bot_task.cancel()` в finally) — не вижу как меня затронет. Просто чистый shutdown. ✓
- **Bug #1** (унифицированный enum payment_method) — AI теперь может правильно создавать ЛК карточки с GUARANTOR_BEFORE/AFTER/AFTER_WORK. Раньше терял детализацию. ✓
- **Bug #7** (TTL cache 5 мин для knowledge) — небольшой буст производительности. Если SIMBA обновит правило — придётся подождать 5 минут. Терпимо.
- **Bug #10** (LK_CARD_STATUSES dict) — пока ничего не меняет в моей работе (только documentation). OK.

## ⚠️ Что меня беспокоит

### A. Bug #9 state machine — WARN-only режим
Если кто-то из менеджеров случайно ставит сделке статус DONE вместо В_РАБОТЕ → ничего не помешает. Будет только лог. **Я бы хотел чтобы кнопка в дашборде показала ошибку "нельзя перепрыгнуть этап"**. Сейчас этого нет.

### B. Bug #10 — статусы ЛК НЕ внедрены
В коде по-прежнему используются только ОТРАБОТАН и ПОПОЛНИТЬ_И_ОТПУСТИТЬ. Статусы из ТЗ (НА_ОФОРМЛЕНИИ, ПЕРЕДАН, СБРОС) — НЕ работают. Worker добавил только документацию. **С моей точки зрения НИЧЕГО НЕ ИЗМЕНИЛОСЬ.**

### C. userbot.py всё ещё 520KB
Worker #3 вынес только 83 паттерна (10KB). Файл всё ещё монолит — рискованно. Это первый шаг из 30+. Признаю — лучше чем ничего, но финал далеко.

## 🚨 Что я проверю РУКАМИ после деплоя SIMBA

1. **Welcome бот** — открыть @PrideInviteWork_bot, нажать /start → должна быть captcha и приветствие
2. **AI ответ** — в work-chat написать "извините за ошибку" → AI **должен НЕ переслать** клиенту (заблокировано regex)
3. **Цена ВТБ** — в work-chat спросить "сколько за ВТБ?" → AI должен ответить **400$** (а не 250, 300, 450, и не молчать)
4. **Дашборд** — открыть https://pride-p2p-production.up.railway.app/admin → JARVIS dashboard, все 13 табов переключаются
5. **CRM бот** — отправить /menu в @PrideOutkup_bot (или какой там CRM) → главное меню появляется
6. **Создание ЛК карточки** — в work-chat AI вызывает create_lk_card → карточка появляется в дашборде с правильным payment_method
7. **State machine** — попробовать update_deal_status на invalid переход → должен быть warning в Railway logs

## 🎯 GO/NO-GO с точки зрения юзера

**🟡 GO WITH CONDITIONS:**

✅ Можно мерджить ЕСЛИ:
1. SIMBA убедился что `storage.lk_prices['втб']` существует (через `прайс показать` в брейн-чате)
2. SIMBA добавил TG-нотификацию owner при tron warmup failure (на случай если monitor умрёт)
3. SIMBA лично пройдёт чеклист из 7 пунктов выше после деплоя
4. SIMBA готов откатить branch одним `git revert` если что-то пойдёт не так

❌ НЕ мерджить если:
- SIMBA не может проверить эти 7 пунктов сразу после деплоя
- Сейчас пик трафика (вечером лучше не катить)

## 💬 Сообщение разработчикам от юзера

> Спасибо за фиксы. Я не разбираюсь в коде, но вижу что:
> - Цены в knowledge теперь не противоречат друг другу — хорошо
> - AI не сходит с ума — хорошо
> - Welcome бот работает — главное
> 
> Но прошу:
> 1. **Алерт мне в TG если tron сломан** — я хочу знать сразу
> 2. **Внедрите наконец полный workflow статусов ЛК** — это уже год обещается
> 3. **Дашборд должен показывать invalid transition ошибку**, а не только в логи писать
> 
> Без этого я не могу спокойно работать.

**Подпись:** оператор PRIDE
